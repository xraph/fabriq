apiVersion: v2
name: fabriq
description: >-
  The fabriq data fabric background plane — outbox relay, projection
  consumers (graph/search), reconciler, and CRDT document materializer —
  plus an advisory-locked schema-migration hook. Datastores are external
  by default.
type: application
version: 0.1.0
# Tracks the fabriq module version; image tag defaults to this.
appVersion: "0.1.0"
keywords:
  - fabriq
  - data-fabric
  - cqrs
  - outbox
home: https://github.com/xraph/fabriq
sources:
  - https://github.com/xraph/fabriq
maintainers:
  - name: xraph
